# Core (training + batch scoring) — minimal, On-Prem friendly
numpy>=1.26
pandas>=2.0
scikit-learn==1.9.0   # pinned: LOGREG_PARAMS uses the l1_ratio API (sklearn >= 1.8)
joblib>=1.3

# Serving API
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# Experiment tracking / registry (optional but recommended)
# Pinned exactly so mlflow / mlflow-skinny / mlflow-tracing stay in lockstep
# (a version skew across them triggers a runtime mismatch warning).
mlflow==3.14.0

# Tests
pytest>=8.0

# Optional tree baselines (not used by the production LogReg model)
# lightgbm>=4.0
# xgboost>=2.0
